<!DOCTYPE html>
<html>
  <head>
    <title>ToukenLog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <%# binding.pry %>
              <% if current_user.nil? %>
                <li><%= link_to "新規登録", new_user_registration_path %></li>
                <li><%= link_to "ログイン", new_user_session_path %></li>
              <% elsif current_user.kind == "admin" %>
                <li>admin</li>
                <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
              <% elsif current_user.kind == "member" %>
                <li>member</li>
                <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
              <% elsif current_user.kind == "guest" %>
                <li>guest</li>
                <li><%= link_to "本登録", edit_user_path(current_user) %></li>
                <li><%= link_to "別のユーザーでログイン", new_user_session_path %></li>
              <% else %>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
